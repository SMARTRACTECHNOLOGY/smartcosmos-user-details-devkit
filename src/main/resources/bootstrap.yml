project:
  artifactId: smartcosmos-generic-service
spring:
  application:
    name: smartcosmos-user-details-service
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}
      failFast: true
